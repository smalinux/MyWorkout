#!/bin/bash

# list 'alll' execuatable files in ur sys

ls /bin  >> ~smalinux/temp
ls /sbin  >> ~smalinux/temp
ls /usr/bin/  >> ~smalinux/temp
ls /usr/sbin/  >> ~smalinux/temp
ls /usr/local/bin/  >> ~smalinux/temp
ls /usr/local/sbin/ >> ~smalinux/temp